{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "root",
  "password": "",
  "database": "nest-tutorials",
  "logging": true,
  "synchronize": true,
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "migrations": ["dist/migrations/**/*.js"],
  "cli": {
    "entitiesDir": "dist/**/*.entity{.ts,.js}",
    "migrationsDir": "dist/migrations"
  }
}
